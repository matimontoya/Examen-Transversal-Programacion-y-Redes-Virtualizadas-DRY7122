all:
  hosts:
    CSR1kv:
      ansible_host: 192.168.48.131
      ansible_user: cisco
      ansible_password: cisco123!
      ansible_network_os: ios
      ansible_connection: network_cli
    localhost:
      ansible_connection: local

  children:
    webservers:
      hosts:
        localhost: